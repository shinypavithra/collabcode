{
  "name": "collabcode",
  "version": "1.0.0",
  "description": "Real-time collaborative code editor (MERN + Socket.IO)",
  "private": true,
  "scripts": {
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install:all": "cd server && npm install && cd ../client && npm install"
  }
}
